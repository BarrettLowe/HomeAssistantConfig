recorder:
  db_url: !secret mariadb_url
  purge_keep_days: 5
  exclude:
    domains:
      - updater
      - script
      - device_tracker
    entities:
      - sun.sun
      - automation.nursery_interval_snapshot