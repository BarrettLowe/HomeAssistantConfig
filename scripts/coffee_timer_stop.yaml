alias: Coffee Timer Stop
sequence:
  - service: scene.turn_on
    target:
      entity_id: scene.music_before_coffee_timer
  - service: input_boolean.turn_off
    data:
      entity_id: input_boolean.coffee_time_alarming